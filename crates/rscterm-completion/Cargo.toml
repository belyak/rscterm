[package]
name = "rscterm-completion"
version = "0.1.0"
edition = "2021"

[dependencies]
rustyline = "13.0"
rscterm-core = { path = "../rscterm-core" }
rscterm-provider = { path = "../rscterm-provider" }
async-trait = "0.1"
tokio = { workspace = true }
tracing = { workspace = true }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
